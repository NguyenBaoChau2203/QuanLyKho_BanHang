## Design

- The sales invoice screen uses standard WinForms panels and a split layout.
- Product lookup and customer lookup are displayed in tabbed search grids for fast selection.
- The invoice editor keeps the line grid, totals, discount, and validation summary visible together.
- All data interactions remain on top of BLL service contracts and deterministic stub data until backend implementations are ready.
